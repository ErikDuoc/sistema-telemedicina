spring:
  datasource:
    url: jdbc:mysql://localhost:3307/doctors_db?allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=UTC
    username: doctors
    password: doctors123
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    serviceUrl:
      defaultZone: http://discovery-server:8761/eureka/
