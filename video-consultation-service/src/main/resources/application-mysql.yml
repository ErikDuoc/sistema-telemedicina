server:
  port: 8091

spring:
  datasource:
    url: jdbc:mysql://localhost:3307/video_consultations_db?allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=UTC
    username: video_consultations
    password: video_consultations123
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: validate
    show-sql: true

appointment-service:
  url: http://appointment-service:8087
