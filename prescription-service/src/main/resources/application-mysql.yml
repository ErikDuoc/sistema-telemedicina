server:
  port: 8089

spring:
  datasource:
    url: jdbc:mysql://localhost:3307/prescriptions_db?allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=UTC
    username: prescriptions
    password: prescriptions123
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true

clinical-record-service:
  url: http://clinical-record-service:8088

eureka:
  client:
    serviceUrl:
      defaultZone: http://discovery-server:8761/eureka/
