spring:
  datasource:
    url: jdbc:mysql://localhost:3307/patients_db?allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=UTC
    username: patients
    password: patients123
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: validate
    show-sql: true

server:
  port: 8081
