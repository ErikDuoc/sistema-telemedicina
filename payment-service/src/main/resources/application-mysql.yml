server:
  port: 8084

spring:
  datasource:
    url: jdbc:mysql://localhost:3307/payments_db?allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=UTC
    username: payments
    password: payments123
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: validate
    show-sql: true
